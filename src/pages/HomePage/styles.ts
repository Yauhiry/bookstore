import styled from "styled-components";

const StyledHomePage = styled.div`
  display: grid;
  align-content: start;
`;

const Container = styled.div`
  display: grid;
  grid-gap: 56px;
`;

export { StyledHomePage, Container };

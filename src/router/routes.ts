export enum ROUTE {
  HOME = "/",
  ACCOUNT = "account",
  BOOK = "/book/:isbn13",
  CART = "cart",
  FAVORITES = "favorites",
  SEARCH = "search/:searchValue/:currentPage",
  SIGN_IN = "/sigh-in",
  PASSWORD_RESET = "/password-reset",
  NEW_PASSWORD = "/new-password",
}
